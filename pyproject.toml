[project]
name = "sift-cli"
version = "0.1.0"
description = "Local offline interactive file search TUI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "textual>=8.2.3",
]

[project.scripts]
sift-cli = "sift_cli.main:main"
